import React from 'react'\n\nconst SkillProficiencyIndicator = ({ proficiency, size = 'md', showLabel = true, animated = true }) => {\n  // Determine proficiency level\n  const getProficiencyLevel = (proficiency) => {\n    if (proficiency >= 90) return {\n      level: 'Expert',\n      color: 'text-green-400',\n      bgColor: 'bg-green-400',\n      icon: '🚀',\n      description: 'Mastery level with extensive experience'\n    }\n    if (proficiency >= 75) return {\n      level: 'Advanced',\n      color: 'text-blue-400',\n      bgColor: 'bg-blue-400',\n      icon: '⭐',\n      description: 'Strong proficiency with solid experience'\n    }\n    if (proficiency >= 60) return {\n      level: 'Intermediate',\n      color: 'text-yellow-400',\n      bgColor: 'bg-yellow-400',\n      icon: '📈',\n      description: 'Good understanding with practical experience'\n    }\n    return {\n      level: 'Beginner',\n      color: 'text-orange-400',\n      bgColor: 'bg-orange-400',\n      icon: '🌱',\n      description: 'Learning and building foundational knowledge'\n    }\n  }\n\n  const proficiencyData = getProficiencyLevel(proficiency)\n  \n  // Size configurations\n  const sizes = {\n    sm: {\n      text: 'text-xs',\n      icon: 'text-sm',\n      padding: 'px-2 py-1',\n      circle: 'w-8 h-8 text-xs'\n    },\n    md: {\n      text: 'text-sm',\n      icon: 'text-base',\n      padding: 'px-3 py-1.5',\n      circle: 'w-12 h-12 text-sm'\n    },\n    lg: {\n      text: 'text-base',\n      icon: 'text-lg',\n      padding: 'px-4 py-2',\n      circle: 'w-16 h-16 text-base'\n    }\n  }\n\n  const sizeConfig = sizes[size] || sizes.md\n\n  return (\n    <div className=\"skill-proficiency-indicator\">\n      {/* Circular Progress Indicator */}\n      <div className=\"relative inline-flex items-center justify-center\">\n        {/* Background Circle */}\n        <div className={`${sizeConfig.circle} rounded-full bg-neutral-slate bg-opacity-20 flex items-center justify-center relative overflow-hidden`}>\n          {/* Progress Fill */}\n          <div \n            className={`absolute inset-0 ${proficiencyData.bgColor} rounded-full transition-all duration-1000 ease-out`}\n            style={{\n              clipPath: `polygon(50% 50%, 50% 0%, ${50 + (proficiency / 100) * 50}% 0%, ${50 + (proficiency / 100) * 50}% 100%, 50% 100%)`,\n              transform: `rotate(${(proficiency / 100) * 360}deg)`,\n              transformOrigin: 'center',\n              opacity: 0.8\n            }}\n          />\n          \n          {/* Icon */}\n          <span className={`${sizeConfig.icon} relative z-10`}>\n            {proficiencyData.icon}\n          </span>\n          \n          {/* Percentage Text */}\n          <div className=\"absolute inset-0 flex items-center justify-center\">\n            <span className={`${sizeConfig.text} font-bold text-light-gray relative z-10`}>\n              {proficiency}%\n            </span>\n          </div>\n        </div>\n        \n        {/* Glow Effect */}\n        <div \n          className={`absolute inset-0 ${sizeConfig.circle} rounded-full ${proficiencyData.bgColor} opacity-20 animate-pulse`}\n          style={{ filter: 'blur(4px)' }}\n        />\n      </div>\n\n      {/* Label */}\n      {showLabel && (\n        <div className=\"mt-2 text-center\">\n          <div className={`${proficiencyData.color} ${sizeConfig.text} font-semibold`}>\n            {proficiencyData.level}\n          </div>\n          <div className=\"text-xs text-neutral-slate mt-1\">\n            {proficiency}% Proficiency\n          </div>\n        </div>\n      )}\n    </div>\n  )\n}\n\n// Linear Proficiency Bar Component\nexport const SkillProficiencyBar = ({ proficiency, skill, showDetails = false }) => {\n  const getProficiencyLevel = (proficiency) => {\n    if (proficiency >= 90) return { level: 'Expert', color: '#10b981', icon: '🚀' }\n    if (proficiency >= 75) return { level: 'Advanced', color: '#3b82f6', icon: '⭐' }\n    if (proficiency >= 60) return { level: 'Intermediate', color: '#f59e0b', icon: '📈' }\n    return { level: 'Beginner', color: '#ef4444', icon: '🌱' }\n  }\n\n  const proficiencyData = getProficiencyLevel(proficiency)\n\n  return (\n    <div className=\"skill-proficiency-bar\">\n      {/* Header */}\n      <div className=\"flex items-center justify-between mb-2\">\n        <div className=\"flex items-center space-x-2\">\n          <span className=\"text-lg\">{proficiencyData.icon}</span>\n          <span className=\"text-sm font-medium text-light-gray\">{skill?.name || 'Skill'}</span>\n        </div>\n        <div className=\"flex items-center space-x-2\">\n          <span className=\"text-xs font-semibold\" style={{ color: proficiencyData.color }}>\n            {proficiencyData.level}\n          </span>\n          <span className=\"text-xs text-accent-cyan font-bold\">\n            {proficiency}%\n          </span>\n        </div>\n      </div>\n\n      {/* Progress Bar */}\n      <div className=\"relative w-full h-3 bg-neutral-slate bg-opacity-20 rounded-full overflow-hidden\">\n        {/* Background Glow */}\n        <div className=\"absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent animate-pulse\" />\n        \n        {/* Progress Fill */}\n        <div \n          className=\"h-full rounded-full transition-all duration-1000 ease-out relative overflow-hidden\"\n          style={{ \n            width: `${proficiency}%`,\n            background: `linear-gradient(90deg, ${proficiencyData.color}, ${proficiencyData.color}dd)`,\n            boxShadow: `0 0 10px ${proficiencyData.color}50`\n          }}\n        >\n          {/* Shimmer Effect */}\n          <div \n            className=\"absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent\"\n            style={{\n              animation: 'shimmer 2s infinite',\n              backgroundSize: '200% 100%'\n            }}\n          />\n        </div>\n        \n        {/* Milestone Markers */}\n        <div className=\"absolute inset-0 flex justify-between items-center px-1\">\n          {[25, 50, 75, 90].map((milestone) => (\n            <div\n              key={milestone}\n              className={`w-0.5 h-2 bg-white rounded-full opacity-30 ${\n                proficiency >= milestone ? 'opacity-60' : 'opacity-20'\n              }`}\n            />\n          ))}\n        </div>\n      </div>\n\n      {/* Details */}\n      {showDetails && skill && (\n        <div className=\"mt-2 text-xs text-neutral-slate\">\n          <div className=\"flex items-center justify-between\">\n            <span>{skill.experience}</span>\n            <span>{skill.projects?.length || 0} projects</span>\n          </div>\n        </div>\n      )}\n    </div>\n  )\n}\n\n// Proficiency Comparison Component\nexport const SkillProficiencyComparison = ({ skills, title = \"Skills Comparison\" }) => {\n  const sortedSkills = [...skills].sort((a, b) => b.proficiency - a.proficiency)\n  const maxProficiency = Math.max(...skills.map(s => s.proficiency))\n\n  return (\n    <div className=\"skill-proficiency-comparison glass-card p-6\">\n      <h3 className=\"text-heading-4 font-semibold text-accent-cyan mb-6 text-center\">\n        {title}\n      </h3>\n      \n      <div className=\"space-y-4\">\n        {sortedSkills.map((skill, index) => (\n          <div key={skill.name} className=\"relative\">\n            {/* Rank Badge */}\n            <div className=\"absolute -left-8 top-1/2 transform -translate-y-1/2\">\n              <div className={`w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold ${\n                index === 0 ? 'bg-yellow-500 text-black' :\n                index === 1 ? 'bg-gray-400 text-black' :\n                index === 2 ? 'bg-orange-600 text-white' :\n                'bg-neutral-slate bg-opacity-30 text-neutral-slate'\n              }`}>\n                {index + 1}\n              </div>\n            </div>\n            \n            <SkillProficiencyBar \n              proficiency={skill.proficiency}\n              skill={skill}\n              showDetails={true}\n            />\n          </div>\n        ))}\n      </div>\n      \n      {/* Summary Stats */}\n      <div className=\"mt-6 pt-4 border-t border-neutral-slate border-opacity-20\">\n        <div className=\"grid grid-cols-3 gap-4 text-center\">\n          <div>\n            <div className=\"text-lg font-bold text-accent-cyan\">\n              {Math.round(skills.reduce((sum, skill) => sum + skill.proficiency, 0) / skills.length)}%\n            </div>\n            <div className=\"text-xs text-neutral-slate\">Average</div>\n          </div>\n          <div>\n            <div className=\"text-lg font-bold text-accent-cyan\">\n              {maxProficiency}%\n            </div>\n            <div className=\"text-xs text-neutral-slate\">Highest</div>\n          </div>\n          <div>\n            <div className=\"text-lg font-bold text-accent-cyan\">\n              {skills.filter(s => s.proficiency >= 75).length}\n            </div>\n            <div className=\"text-xs text-neutral-slate\">Advanced+</div>\n          </div>\n        </div>\n      </div>\n    </div>\n  )\n}\n\nexport default SkillProficiencyIndicator"